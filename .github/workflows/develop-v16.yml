name: V1.6 Symmetric Development

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - develop_v16.py
      - test_v16.py
      - .github/workflows/develop-v16.yml

permissions:
  contents: read

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Restore development cache
        uses: actions/cache/restore@v5
        with:
          path: backtest_data_v142_oos
          key: v142-oos-data-${{ github.run_id }}
          restore-keys: |
            v142-oos-data-
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy requests
      - name: Run unit tests
        run: python -m unittest -v test_v16.py test_v15.py test_v14.py test_v142.py
      - name: Run V1.6 development only
        env:
          TWELVE_DATA_API_KEY: ${{ secrets.TWELVE_DATA_API_KEY }}
        run: python develop_v16.py
      - name: Upload V1.6 development results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v16-development-results
          path: backtest_results_v16_development/
          if-no-files-found: warn
